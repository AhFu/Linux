function pd_test (){
if [[ $# -eq 0 ]]
then
return
fi
echo "ipnut is $@"
pushd $@
local dirs=($(pushd))
for(( i=0;i<${#dirs[@]};i++ )); do
dirs[i]="${red}$i)${normal}${dirs[i]}"
done
echo -e ${dirs[@]}
}

